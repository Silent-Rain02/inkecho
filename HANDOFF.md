# InkEcho 接手说明

InkEcho 是一个面向任意上传小说的中文续写与内容问答工作台，《蛊真人》是当前主要验收样本。项目当前是无构建步骤的原生前端 + Python 标准库服务端，适合直接在本地继续迭代。

## 本地运行

章节导航首次加载最多保留 3000 个标题；输入至少两个字符后会按关键词扫描当前知识空间在 20,000 个资料片段容量内的完整本地章节索引，避免超长小说的后段章节无法定位。结果只返回安全标题，不返回正文；兜底请求失败时会明确显示服务不可用，不会把失败误报成“没有匹配”。
章节导航列表默认只渲染 40 个可见结果；命中更多标题时，用户可点击“加载更多”逐批展开，避免长篇小说一次性占用大量页面布局。
章节导航会保留服务端归一化后的远程命中结果；输入 `Chapter One`、`Epilogue` 等英文标题标记时，不会被前端二次筛选隐藏。

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 server.py
```

默认打开 `http://localhost:5173`。如果端口被旧实例占用，可以用 `INK_ECHO_PORT=5174 python3 server.py` 启动；当前已验收的新界面预览为 `http://127.0.0.1:5174/#home`。模型密钥只放在本地 `.env`，参考 `.env.example`，不要提交真实密钥。

将用户自己的《蛊真人》文本路径写入 `.env`：

```bash
INK_ECHO_SOURCE_NAME=蛊真人
INK_ECHO_SOURCE_FILE=/绝对路径/新蛊无删减错别字修正5.0.txt
```

原文是本机只读知识库，不应复制进仓库或交接文本；服务端只返回有限的检索片段给模型上下文。

### 当前阶段重点

- 主路径坚持“选择 / 上传小说 → 选择内容问答或续写 → 输入并发送”，常用任务不依赖滚动寻找模块。
- 一级页面切换会回到目标页顶部，避免从长对话返回首页或小说库时落在页面中段；草稿和当前项目状态不受影响。
- 新小说解析可用时会直接进入该小说的任务首页，用户接下来只需“选择问答或续写 → 输入 → 发送”；正文为空时才进入原作恢复页，不让正常用户先阅读解析诊断。
- 办公网 Azure、OpenAI、Azure OpenAI 和兼容服务在当前标签页首次发送前显示应用内确认框；只有点击“继续并发送”才会把命中的有限原作片段与必要上下文交给模型。取消不会新增消息、清空草稿或发送请求，Ollama 本机路径不弹出远程数据确认。
- 生成、摘要和重试在数据授权前先调用本机 `/api/health` 预检模型配置；缺少字段或服务不可达时保留草稿并切到模型设置，不新增消息、不弹远程数据授权，也不发出模型请求。
- 模型设置的切换、刷新列表和测试连接也会先做同一套预检。已知缺少配置时只显示“待配置”、可理解的缺少项和三步修复指引，不请求 `/api/models` 或 `/api/probe`，不会把未配置误报为连接失败；技术键名只在“复制缺少项”和连接诊断中提供。
- 回复正文里的章节引用和检索召回列表是两层信息：前者会核验，后者明确标为“检索候选”，默认只展示两项，避免把召回章节误当成答案证据。
- 通用小说的检索质量不依赖《蛊真人》词表；首个证据片段覆盖至少三个有效检索词时可判为“命中充分”，只有单一实体或零散弱命中时仍保持保守提示。
- 续写查询中的“继续 / 续写 / 下一幕 / 结尾”等操作词不会参与正文相关性打分，防止下一章因偶然出现“继续”而被误标为关键词命中；显式章节后的相邻篇章必须显示为“章节接续桥”。
- 显式章节续写采用混合检索：当前章末尾和下一章桥必须保留，同时补充用户点名设定的同卷邻近章节；跨卷但章节号相同的结果不能冒充当前章节。接续桥与所有位于锚点后的支持片段在模型提示中标记为“当前尚未发生 / 未来参考”，只用于避免矛盾，禁止把未来持有物、知识、能力和行动倒灌到当前场景。
- 数值问题会识别预期单位和问题主体，把精确数值所在窗口置于证据中心；精确数值加权必须要求角色 / 主体同窗命中，不能因为另一个人物在同一事件中出现了精确数值就误召回。
- 事件结果问题会从事件首次可信出现处向后检查最多六个章节，并优先排序含明确结果陈述的答案窗口。真实办公网复测中，“方源首次开窍测出的资质”已从错误的“目前不确定 / 甲等预测”修正为“丙等资质”，首条依据为第一卷第六节，引用核验通过。
- 问答现在把 `source_answer_coverage` 与 `source_quality` 分开：前者表示片段是否真正包含答案陈述，后者只表示召回相关性。前端、重试版本、复制 / 导出和依据弹窗都保留这两层状态；“仅主题相关”即使检索质量为强也会给出换策略提示。
- 问答检索不再自动追加创作项目的“当前章节 / 场景”。此前这会把全书事实问题静默限制到第六节，并让真实模型误判“丙等资质”指向不明；修复后同一真实问题准确回答丙等，并引用第五、六节。续写仍保留章节锚点、下一章接续桥和未来边界。
- 通用小说的因果问答新增“因果答案邻近命中”：只有同时覆盖至少两个有效问题词并出现明确因果连接词的片段才获得答案加权；回归样本中，真正说明“因为城门封锁”的段落会排在多次提及“离开旧城”但未说明原因的段落之前。
- 《蛊真人》专项收口时修复了同章人物数值串线：短问法“方源开窍后元海占空窍多少”曾把方正的“八成”窗口排在前面，因为“方源”出现在数值后的比较句中。现在主体必须在数值前建立，提取出的答案值会成为证据窗口硬锚点；真实办公网模型复测回答“四成四（44%）”，引用第一卷第六节且引用核验通过。
- 《蛊真人》长文本当前解析指标为 7,501,829 字符、2,344 个章节单元、5,048 个资料片段；解析状态就绪且没有警告。原文不在仓库内。
- 2026-08-11 已在用户明确授权后完成真实办公网模型验收；最小探测约 2.2 秒成功，原作问答准确回答“丙等资质”并引用第五、六节，续写返回 309 字、未截断且轻量质检通过。应用内数据去向确认仍只授权当前标签页，新的浏览器标签页应继续尊重该边界。

### 2026-08-11 阶段评审

- 产品经理视角：主路径已经满足“选书 / 上传 → 选任务 → 填入或输入并发送”的三步结构。新版隐藏右侧灵感栏曾造成首次任务无起点，现已通过仅首次显示的快捷起点修复。续写轻量质检已落地并遵循“默认隐身、异常才提示”，没有在首页或工作台增加常驻模块。
- 真实用户视角：问答从最初只返回资质范围“四五成”，经过数值主体匹配、精确答案窗口和事件邻近召回后，能够返回角色本人“四成四”；证据链可信度明显提升。续写从通用仙侠设定混入、未来道具倒灌和截断，修正到设定可接受、时间线不提前、输出完整；轻量质检现在能识别提纲化、写作说明、长篇分析但动作 / 对话不足，以及把后续事物写成当前已持有。普通未来规划和动作充分的场景样例不会误报。
- 已验证的硬门槛：不伪造模型失败内容；失败提示不进入上下文；问答引用可核对；章节续写保留末尾与下一章桥；未来支持片段不等于当前状态；GPT-5 类模型约 600 字输出不因隐藏推理预算被截断。
- “续写需复核”详情已提供“按建议优化”：只有最后一条可重试回复显示该动作；前端只发送最多四个白名单风险码，服务端映射为固定改写要求，拒绝任意提示文本。优化复用原有重试、失败回退和多版本机制，原回复不会被自动覆盖。
- 定向优化结果现在会比较目标风险与新版本质检：全部消失显示“质检已通过”，部分消失显示“风险已减少”，未改善显示“仍需复核”。状态只附着在定向优化产生的具体版本，切回原版或普通重试不会误标；完成时另有一次短暂反馈，长期状态仍收在版本切换区。
- 恢复路径审计结果：干净首次访问可从首页三步内进入问答 / 续写；原文不可用时小说库与原作页都有单一恢复动作；模型未配置时不再出现“待配置”和“连接失败”两个相互冲突的状态。产品经理视角下一阶段不应再增加常驻控制项，优先继续提升答案证据充分度；真实用户视角下一阶段应让“为什么可信”比“还能设置什么”更容易理解。
- 阶段状态：截至 2026-08-11，《蛊真人》作为 base 的解析、内容问答、章节续写、证据分级、引用核验、模型接入和异常恢复主链路已完成专项验收，可阶段性停止。任意小说的跨段指代消解与轻量纠错反馈保留为后续恢复开发时的首要 backlog，不属于本次《蛊真人》收口门槛。

## 主要文件

- `index.html`：页面结构、对话菜单、角色卡（含人物设定）、场景计划、检查点和模型服务操作入口
- `styles.css`：整体视觉、响应式布局、模板卡片和场景计划交互状态
- `app.js`：项目状态、场景计划、浏览器本地保存、流式对话和全部前端交互
- `server.py`：模型服务适配、请求超时、安全静态资源和 HTTP 路由
- `test_server.py`：服务端配置、路由、安全、流式增量输出和模型请求回归测试
- `test_frontend_contract.py`：前端 HTML id、静态资源和关键控件契约检查
- `README.md`：用户向项目说明

## 产品主链路

跨设备导入项目后，小说库会列出待恢复的小说空间；恢复提示提供明确的小说空间选择器，上传文件名可以与原文件不同，系统仍按所选空间名称恢复项目与检查点绑定。

1. 首次打开前端会先进入小说库，用户从当前知识空间卡片选择“续写”或“内容问答”；已有会话会恢复上次浏览页面。工作区导航下会持续显示当前小说名称和空间就绪状态；正文为空、解析提醒或原文不可用时会直接显示原因，帮助用户在多部小说间保持边界。工作台顶部引导会根据当前空间状态动态指向小说恢复或原作资料检查。
   - 小说库入口直接提供“开始续写 / 内容问答 / 查看资料”三条核心路径；空间原文未就绪时，内容问答按钮会禁用并解释恢复条件，续写仍可继续。
   - 当前小说已就绪时，顶部引导会直接露出“内容问答 / 开始续写”两个高频任务按钮；原作资料、空间记忆、故事管理和模型设置收进“更多工具”，保留页面切换但减少首屏导航噪音。
   - 小说库首屏现在把高频入口收敛为“内容问答 / 开始续写 / 查找原作”三个任务按钮；空间状态、搜索筛选、导入导出等管理信息默认折叠，用户先按目标进入任务，再按需展开内部管理。
   - 顶部“功能菜单”改为按任务搜索：续写、内容问答、查找原作、维护空间记忆、管理场景与角色、配置模型服务均可直接跳转；保留 ⌘ / Ctrl + K 作为快速打开方式。
   - 当前前端采用全新布局：左侧固定当前小说卡和工作区导航，右侧展示当前任务主区；小说库、工作台和辅助页面共享同一信息架构，避免用户在横向模块导航和多层说明之间来回寻找。
   - 工作台采用“会话历史侧栏 + 当前对话”结构。切换续写、内容问答、改写或角色独白时，当前会话会保存为独立快照，新功能从空白会话开始；历史会话可切回且各自保留草稿与对话上下文。
   - 工作区引导下方会同步显示当前空间的四阶段进度：原文、解析、空间记忆、创作；解析提醒会标为“需确认”，没有记忆会标为“可选”，尚未建立项目会标为“准备开始”，避免用户把可选记忆误认为使用前置条件。
   - 四个进度阶段都是可点击快捷入口：原文 / 解析会按当前空间是否可用进入原作资料页或小说库恢复页，记忆会定位到空间记忆编辑器，创作会直接回到工作台。
   - 工作区页面现按小说库、工作台、原作资料、空间记忆、故事管理和模型与数据分区；空间记忆有独立 URL 页面，负责长期知识维护，原作资料页只负责章节核对与检索依据。
   - 专注模式只作用于工作台；切换到小说库、原作资料或其他分区时会自动退出，并通过 `hidden` 状态确保被隐藏的工作台不会再次撑开页面。
   - 空间记忆区的“刷新记忆”会强制重新读取当前小说空间，适合多窗口 / 多页面协作；若编辑器有未保存内容会先确认，刷新后顶部四阶段进度也会立即更新。
   - 记忆来源筛选新增“待核对原文”；原文版本变化后可集中查看失效的原作依据 / 章节摘要，列表仍提供“重新核对”入口，服务端不会把这些过期来源带入续写。
   - 空间记忆页提供带确认的“清理待核对”操作，可一次删除当前空间已失效的原作依据 / 章节摘要，保留手动笔记和最新连续性记忆；未保存的编辑内容不会被删除。
   - 跨设备交接后如果原文尚未恢复，带原文版本的来源记忆会标记为“原文未就绪”并隔离出续写上下文；点击重新核对会先回到小说库恢复原文。
   - 空间记忆前端缓存同时记录对应的原文 `revision`；刷新小说空间列表时若 revision 变化，即使记忆数量和写入时间未变化，也会主动清除旧缓存并重新读取，避免过期状态被旧缓存遮蔽。
   - 后端小说空间状态快照会一并持久化原文 `revision`；服务重启后从轻量快照恢复状态时，前端仍能获得同一版本标识。
   - 原作资料页诊断区域在当前空间有可检索正文时提供“查看解析样本”；它调用 `/api/source/sample`，最多读取 12,000 字本机提取内容，适合核对无标题 / 章节识别有限的 TXT、DOCX、EPUB、FB2 或 PDF。
   - 同一小说空间存在多个创作项目时，小说卡片显示项目数量；从卡片进入续写会优先恢复当前项目，否则按 `updatedAt` 打开最近更新的项目，避免误回到最早分支。
   - 已上传小说空间支持重命名；重命名只更新空间注册表中的显示名称，不改动原文、内部文件名、章节索引、空间记忆或项目绑定。
   - 小说库的上传新小说、恢复原文和空状态入口均支持拖拽文件；拖拽与点击选择共用同一套异步任务、重试和目标空间绑定逻辑。
   - 多卷上传会把自然排序后的原始文件名保存到空间元数据；`source.source_files` 会随 `/api/novels` 返回，小说卡片可展开查看完整分卷清单，服务重启后仍能确认当前空间包含哪些文件。
   - 原作资料页诊断会显示“单文件原文”或“X 个分卷”，在章节核对时明确当前检索范围。
   - 解析器会把 `楔子 / 引子 / 尾声 / 后记 / 间章` 以及英文 `Epilogue / Afterword / Interlude` 归一化为独立章节边界，避免这些段落被并入上一章；解析器版本变化会自动使旧索引失效并重建。
   - 小说库搜索会把 `source.source_files` 中的每个原始分卷文件名纳入匹配，按任意分卷名称都能找回对应的统一知识空间。
   - 小说库搜索或状态筛选无结果时，空状态会直接提供“上传新小说”入口，用户不需要先恢复完整列表才能继续建立知识空间。
   - 小说库提供本地搜索和状态筛选，可按名称、文件名或解析提示定位空间；筛选只影响展示，不改变当前空间和项目绑定。
   - 小说空间的 `lastAccessedAt` 保存在浏览器本地元数据中；选择空间时更新，服务端刷新合并时保留，列表按当前空间 > 最近访问 > 空间更新时间排序。
   - 上传空间同时保存内部安全文件名与用户原始文件名；小说库刷新或服务重启后仍展示原始文件名，删除 / 解析仍只使用内部文件名。
   - 对升级前缺少原始文件名字段的旧空间，前端同步会优先回填本地缓存中的可读文件名；新上传则以服务端持久化的原始文件名为准。
   - 搜索或状态筛选生效时会显示“清除筛选”快捷操作，空结果状态可以直接恢复完整空间列表。
   - 小说库工具栏会汇总所有空间的可检索、解析提醒、正文为空和待恢复数量；该摘要不受当前筛选影响，反映整体健康度。
   - 原作资料页的诊断区域会同步当前空间的正文规模、章节 / 片段数量、编码、问答能力和解析提醒，避免用户在查看依据时丢失空间状态。
   - 模型生成或摘要提炼期间会阻止小说空间切换、原文上传 / 重解析和空间删除，避免异步回复写入错误项目；下拉切换被阻止时会恢复当前空间选项。
   - 切换小说空间会递增章节导航、章节阅读和原作依据的请求代号并关闭旧资料弹窗；响应还会复核请求空间 ID，防止旧空间异步结果残留。
   - 回复、剧情摘要、本幕结果和原作章节摘要写回前都会复核请求发起时的项目 / 小说空间；校验失败时只放弃写入，不会污染当前项目。
   - 诊断区域还会显示平均片段长度与章节上下文覆盖率（有明确卷 / 章 / 节标题的分片占比）；服务端若只能使用编码容错读取，会将转换为 UTF-8 后重新解析的建议写入解析提醒。
   - HTML / DOCX / EPUB / FB2 / PDF 在本机归一化为检索文本后，诊断仍使用上传登记的原始解析格式（`html` / `docx` / `epub` / `fb2` / `pdf`），不把它们误显示成普通 TXT 的 UTF-8 编码。
   - 资料页诊断异常时会显示直接操作：上传空间选择修正版原文重新解析，其他空间跳转小说库恢复原文。
   - 原文待恢复或正文为空时，原作资料页会同步禁用查看依据、阅读章节和内容问答入口，并把“恢复 / 重新解析”作为明确下一步；章节定位仍可先记录到工作台。
   - 原作资料页支持从当前章节直接阅读本机原文预览；服务端按精确标题匹配，每次最多返回 12,000 字，未知标题返回 404，预览不会写入项目或空间记忆。
   - 章节阅读器返回相邻章节标题并提供“上一章 / 下一章”按钮；首尾章节会隐藏对应按钮，切换始终通过当前小说空间重新读取原文。
   - 章节阅读器的“围绕本章提问”会关闭原文弹窗，调用 `beginSourceQuestion("", title)` 带回章节定位，并切换到事实优先内容问答。
   - `renderSourceReferences()` 会把回复下方的已检索章节标题渲染为可点击阅读入口；点击前会去掉无标题长文本的展示后缀“· 片段 N”，再交给章节阅读器精确匹配。
   - 章节预览可以调用当前模型生成“原作章节摘要”草稿；摘要不会自动写入，用户审核后才会以 `source_summary` 类型保存，并在续写上下文中标记“模型整理，需以章节原文为准”。
   - 原作依据与章节摘要会保存当前原文版本指纹；小说重新解析或原文被替换后，旧来源记忆会标记 `source_stale`，前端显示“原文已更新，请重新核对”，服务端也不会再把它带入续写上下文。
   - 过期来源记忆卡片提供“重新核对”，会定位到 `source_chapter` 对应的当前章节；同类型、同标题、同来源的重新生成会更新原条目，不会重复新增。
   - 所有带 `source_chapter` 的原作依据 / 章节摘要记忆都提供“查看原文”；无标题来源的“作品开篇”会打开解析样本，原文版本变化后自动切换为“重新核对”。
   - `novel_space_memory_summary()` 返回 `stale_count`；小说库卡片会在空间状态中显示“X 条原文记忆待核对”，记忆读取和写入后都要同步刷新该摘要。
   - 工作区页面会写入当前浏览会话并同步到 URL hash（例如 `#source`、`#story`）；多小说场景会附带空间 ID（例如 `#source/novel-…`），刷新、复制链接或浏览器前进 / 后退后都能回到对应页面和小说空间，首次打开仍以小说库作为主入口。
   - 首屏 HTML 与脚本初始化状态保持一致，首次加载不会短暂闪现工作台；工作区标签支持方向键、Home / End 键盘切换。
   - 小说空间列表在服务端返回前保持“正在读取”状态，只有请求完成或失败回退后才显示空间卡片或空态，避免加载竞态造成误导。
   - `/api/novels` 读取失败时会区分“服务暂不可用”和真正空库：已有本地缓存继续展示，没有缓存则显示刷新重试引导；上传成功后会清除失败态。
   - 工作区导航提供当前小说快速切换，下拉选择会保留正在查看的页面，并与小说库统一按“当前空间 → 最近访问 → 空间更新时间”排序；切换前仍会检查未保存的空间记忆草稿。
   - 当前空间 ID 会先校验项目绑定是否仍存在；旧项目引用已移除或待恢复空间时，检索、问答和空间记忆会安全回退到已知活动空间 / 默认空间，不会出现界面显示默认小说但请求仍使用失效空间 ID 的串库状态。服务端空间列表刷新后也会重新对齐当前项目绑定。
   - `/api/novels` 成功返回但缺少某个本地缓存上传空间时，前端会将该空间转为“原文待恢复”，清空过期的正文 / 章节 / 片段指标；重新上传同名原文仍可走原空间恢复流程，避免旧缓存继续伪装成可检索知识库。
   - 待恢复上传空间的小说卡片直接提供“恢复原文”；正文为空或解析提醒空间提供“重新解析”，入口会显式绑定目标空间并保留空间 ID、项目和记忆。
   - 待恢复卡片将“恢复原文”置为主操作，无原文续写与资料查看降为次级入口；恢复、问答和资料按钮的无障碍标签会带上小说名，避免多个空间同时出现时难以区分。
   - 卡片使用统一的空间就绪状态区分“可检索、解析提醒、正文为空、待恢复”，并分别提示续写 / 内容问答是否可用；文件存在但没有正文时不会再显示为可问答。
   - 发送内容问答前的 `ensureSourceReadyForMode()` 复用同一套 `getNovelSpaceReadiness()` 判断，卡片能力提示与实际发送拦截保持一致。
   - 空正文或解析提醒的已上传空间提供“重新解析”按钮；选择文件后前端会再次确认，服务端仅在显式传入目标空间 ID 和 `replace_existing` 时替换现有原文，保留空间 ID、项目绑定与空间记忆，并清理旧检索缓存。
   - 重新解析失败后的“重试解析”会保留目标空间 ID；普通上传入口会主动清除该目标，避免重试状态污染下一本小说。
   - 上传任务轮询超时或状态接口短暂不可用后，小说库会保留本地后台任务引用并禁止重复上传；“刷新状态”会先刷新空间列表，再恢复任务，若任务已完成会自动接收结果并清理任务引用。
2. 服务端从 `INK_ECHO_SOURCE_FILE` 加载文本，按卷 / 章 / 节标题切分并缓存。
   - 小说库上传支持 TXT、Markdown、HTML、DOCX、EPUB、FB2 和 PDF；上传新小说时可以填写可选的知识空间名称，留空则使用文件名，多卷上传也可以用一个统一作品名；恢复或重新解析已有空间时保留原空间名称。前端将所有格式按原始字节以 Base64 上传，避免中文文本在 JSON 请求体中额外膨胀，也避免文档结构被浏览器文本解码破坏；服务端再按后缀识别编码或提取正文。HTML / EPUB 使用本机标准库过滤脚本 / 样式等非正文节点，EPUB 按阅读顺序读取并保留常见块级段落换行，DOCX 提取正文段落，FB2 使用标准库 XML 解析正文标题和段落；PDF 使用 `pypdf` 提取文本，扫描版 PDF 仍会明确提示暂不支持 OCR。
   - 小说库支持一次选择或拖入多个小说文件；前端会按文件名自然排序，服务端把每个文件作为独立分卷边界合并到同一个知识空间，章节标题不会因跨文件重复而相互覆盖；单个文件和合并总大小均不超过 20 MB，一次最多 32 个文件。
   - 多卷解析会保留“文件边界 · 内部卷 · 章节”的层级标题；即使每个分卷都从“第 1 卷 / 第 1 章”重新编号，检索和章节导航也不会把不同文件的同名章节合并。
   - 点击选择和拖拽上传共用前端格式校验；不支持的扩展名会在请求发出前直接提示，避免无效文件占用上传和解析时间。
3. 每次请求根据用户问题、当前章节和场景目标做轻量词法检索：默认《蛊真人》空间才启用其领域词、实体组合和开篇篇章加权；其他小说只使用通用词项、标题命中、实体关系和按知识库频次调整的稀有度评分，避免示例小说词表跨空间污染；如果查询里出现明确的“第 X 节 / 章 / 卷”，对应标题会获得额外权重，再把有限片段加入模型系统提示。
   - `source_chunks()` 会按 UTF-8、UTF-16、GB18030 顺序尝试解码本地原文，不能解码时才退回带 `errors="ignore"` 的 UTF-8；`source_status()` 会返回实际编码并由前端显示，不要把编码失败静默当成正常全文读取。
   - 章节识别兼容“第两百章”“第〇一章”等中文数字编号；章节定位和引用核验会使用同一套数字归一化规则。
   - `source_query_from_payload()` 会识别“继续写下去”“为什么”等低信息追问，回溯最近一条有内容的用户问题并与当前章节 / 场景目标合并，保持续写和问答追问的原作依据连续。
   - `source_search()` 会按当前切分结果缓存最近查询；同一请求中的提示构造与依据元数据不会重复计算，重新加载或修改原文后缓存会失效。
   - 章节索引会在本机 `.inkecho-data/.indexes` 保存为带版本的派生缓存，并按解析器版本、原文路径、修改时间和文件大小校验；服务重启后可直接恢复分片，解析器升级或源文件变化时会自动重建，原文不会写入浏览器或 Git。
   - 小说库会为上传空间保存不含正文的解析状态快照；服务重启或刷新列表时先按解析器版本、文件大小 / 修改时间校验快照，避免为每个空间重新加载完整索引，实际检索仍按需读取索引。
   - 长文本分片会在不超过单片上限的前提下优先落在段落换行或句末标点，减少检索依据和续写承接被从句中间截断的情况。
   - 没有章节标题的长 TXT / Markdown 会将多个“作品开篇”片段作为独立检索证据返回，并在引用标题中标出片段序号，避免标题去重导致后文命中丢失。
4. 内容问答是独立的原作问答模式：默认《蛊真人》空间的界面标题和引导保留原作专属内容，其他上传小说会自动使用当前小说名称、通用提示和通用快捷灵感；新建其他小说项目不会注入方源等默认角色。所有空间的回复身份显示为 InkEcho 资料助手，不进行当前角色扮演；服务端会隔离当前角色卡、场景计划、参考片段、剧情摘要和创作要求，只保留作品 / 章节定位，并使用独立的资料助手身份、问答篇幅和事实优先提示，前端也会锁定并显示“事实优先”。回答优先依据检索片段并标注“原作依据 / 合理推断 / 目前不确定”；具体事实会被要求尽量标注“依据：章节标题”，不得用模型记忆把多个片段拼成未被当前片段直接支持的结论；服务端还会把检索命中强度写入问答提示，命中有限或未命中时收紧事实边界。续写只借用人物、设定和事实，不直接复制原文。
   - `createProject()` 对自定义小说缺省字段使用通用角色和通用开场；即使用户把自定义空间重命名为“蛊真人”，输入提示和资料助手身份仍按空间 ID 判断，不会误触发默认空间专属逻辑。
   - 前端发送问答前会检查当前空间是否存在可检索正文；原文缺失或解析为空时会保留草稿并返回小说库恢复空间，续写不受此限制。
   - 问答上下文和检索查询会双向隔离模式：问答只保留带问答标记的历史，兼容早期没有模式标记的创作历史；续写 / 改写 / 独白会排除问答历史，避免跨模式内容污染原作依据。
   - 小说库卡片同时展示正文规模、章节单元、资料片段、编码和解析提醒；解析状态会区分可检索、解析提醒、正文为空和待恢复，并分别说明续写 / 内容问答能力。解析提醒来自服务端 `parse_diagnostics.warnings`，用于判断当前空间是否适合直接进行内容问答。
   - 小说列表归一化会保留服务端返回的 `stale_count`，因此未进入的其他小说空间也能即时显示待核对原文记忆数量；空间配置导入同样使用统一的记忆摘要结构。
   - 空间记忆支持 `pinned` 标记；前端可置顶 / 取消置顶，服务端续写记忆选择会将置顶笔记加权到最前并在上下文标签中标记“已置顶”，问答模式仍完全排除这些笔记的事实语义。
   - 空间记忆页会显示全部、原作资料、连续性 / 二创和待核对四类数量；点击统计卡即可快捷筛选，方便长期维护较多笔记。
   - 记忆搜索和来源筛选会显示当前筛选状态，并提供“清除记忆筛选”一键恢复全部笔记。
   - 原作资料页支持“预览本次记忆”：发送前可查看当前续写实际会带入的空间笔记、命中原因和笔记来源；内容问答会明确显示不会使用空间记忆。
   - 原作资料页新增“进入内容问答”快捷入口；如果已填写章节定位，会先同步到项目再切换问答模式并回到工作台。
   - 原作依据弹窗中的每个片段都提供“带回内容问答”，会把当前查询和片段章节定位带回工作台并自动切换事实优先模式，减少从证据核对到提问的手动跳转。
   - 原作依据弹窗中的每个片段都提供“阅读章节”，使用服务端返回的原始章节标题打开有限原文预览；无标题长文本的“作品开篇 · 片段 N”也会回到统一的作品开篇原文。
   - 原作检索依据弹窗中的每个片段都可以一键保存为空间记忆；保存内容会带上小说名、章节和核对问题，并以 `source_evidence` 类型单独标记。
   - `source_evidence` 还会保存真实章节标题、同章分片序号 / 总数和原文版本；展示标题带有“作品开篇 · 片段 N”时，记忆仍保留可追溯的原始章节信息。
   - 空间记忆选择按更新时间排序，低信息续写追问的兜底笔记优先取最新记录，置顶笔记仍保持最高优先级，避免存储顺序造成旧记忆误注入。
   - 自动生成的“剧情摘要 / 本幕结果”按类型、标题和来源更新已有条目，不会反复提炼就持续创建重复版本；手动记录仍保持新增行为。
   - 服务端 `write_novel_memory()` 同样会合并导入或旧客户端传入的重复自动记忆与原作依据记忆，保留首条 ID、最早创建时间和置顶状态，避免绕过前端造成记忆上限膨胀。
   - 续写记忆选择顺序为“置顶笔记 > 与当前查询有重叠的笔记 > 最近两条无命中兜底”，较早且无关的空间笔记不会持续占用模型上下文。
   - 相关命中分数中，`source_evidence` 高于剧情摘要 / 本幕结果、低于手动记录；置顶笔记仍拥有最高优先级。
   - 空间记忆卡片支持编辑；保存更新时复用原笔记 ID，并保留 `kind`、`origin`、`created_at` 和 `pinned` 字段，避免修正规则时丢失来源和优先级。
   - 空间记忆编辑器提供“人物关系 / 世界规则 / 二创约定”快速模板；套用模板前会保护未保存草稿，内容区显示 4000 字上限进度并在接近上限时提醒。
   - 记忆列表会把置顶笔记排在最前，并实时提示续写的记忆选择规则；内容问答明确不读取空间记忆，避免把辅助笔记误认为原作依据。
   - 原文已更新或缺失的来源记忆在重新核对前不会被续写选中，也不会允许置顶，避免用户误以为置顶可以绕过原文失效状态。
   - 续写提示会按记忆类型标注 provenance：`source_evidence` 显示“已核对备忘，以本次检索为准”，手动记录 / 摘要 / 本幕结果显示为连续性或二创辅助；当前检索片段优先级更高。
   - 空间记忆写入携带 `expected_updated_at` 版本；服务端检测到其他页面已更新时返回 409，前端刷新当前记忆并阻止旧数据覆盖。
   - 刷新小说库时前端会对比服务端返回的空间记忆版本与数量；发现其他页面已更新后会清除对应缓存并重新读取，避免刷新状态后仍显示旧笔记。
   - 从项目下拉菜单切换到另一部小说时，`hydrateActiveProject()` 会同步知识空间、检索策略、空间记忆和小说库高亮；如果记忆编辑器存在未保存草稿，切换前会要求确认，避免笔记跨空间串写。
   - 服务端空间记忆上限为 100 条；前端在新增手动笔记或写入摘要 / 本幕结果前先检查上限，达到上限时只提示编辑 / 删除，不允许静默截断旧笔记。
   - 空间配置导入和服务端记忆接口也会拒绝超过 100 条的输入，避免绕过前端后发生静默截断；导入包还会携带原始文件名、格式和正文规模摘要，导入到可能不同版本的同名小说时会在确认框中提示原文不匹配风险；导入方需要先在原设备整理记忆。
   - 空间配置导入成功后会清空记忆编辑器；移除当前小说空间前会保护未保存笔记，避免删除或切换后残留旧空间草稿。
   - 服务端会将问答输出中的“依据 / 参考”章节与本次检索结果做轻量核对；不在结果内的章节会通过 `source_citation_status: "unverified"` 和 `source_citations_unverified` 返回，前端显示“引用待核对”徽标；没有明确章节引用时，问答会显示“引用未标注”。
5. 若知识库或模型服务不可用，前端明确显示配置 / 未生成状态，不用本地模板编造续写或原作结论。
6. 模型流式响应的 `start` 事件会携带服务端最终采用的 `source_query` 和有限的章节标题 `source_references`，前端以服务端查询为准显示并保存依据；原文片段不会写入浏览器项目数据。
7. 点击回复下方的“查看依据”会调用本地 `/api/source/search`，在临时弹窗中展示围绕命中词截取的有限证据窗口、检索强度和查询，并支持复制；面板明确提示检索强度不等于答案事实置信度，关闭弹窗后不会保存这些片段。
8. 发送前也可以点击输入框旁的“先检索原作依据”，用当前草稿（或最近问题）打开同一个临时依据面板；这不会创建消息或写入项目历史。
9. 回复会持久化本次请求的 `sourceQuery`，依据面板优先使用它而不是当前已变化的章节字段；检索结果按章节标题去重。
10. Markdown 导出和项目交接摘要会保留 assistant 消息的安全原作章节标题与依据查询，不包含原文全文；如果模型失败，也会保留查询词方便复核。
11. “复制当前对话”同样会带上安全原作归因，方便直接粘贴给下一位 agent。
12. 模型流式生成期间，模式和角色卡会暂时锁定；这是为了避免生成请求与当前界面设定发生串线，停止或完成后会恢复。
27. 流式生成期间还会锁定项目选择、模型服务、模型名、创作倾向和回复长度；`setSending(false)` 后恢复，避免等待期间改动的设置被误认为属于当前请求。
13. 普通完整上下文模式会从归档历史保留最近 4 条消息，再拼接当前工作区最近消息，作为跨归档边界的连续性桥接；精简上下文模式仍只发送剧情摘要和最近两轮，摘要 / 场景提炼仍可读取完整历史。
14. 问答引用核验状态支持多版本回复：`sourceCitationStatuses` 和 `sourceCitationsUnverifiedByVersion` 会按版本保存；切换版本时必须同步当前徽标，导出 / 交接使用当前版本的“引用待核对”信息。
15. 最后一条被服务端标记为 `truncated` 的回复会显示“展开重试”；点击后向流式接口单次传入 `response_length: "expanded"`，不改变项目保存的默认回复长度。
16. 每条消息会保存生成模式；重试旧回复时使用原消息模式，不受当前模式 tab 影响；消息徽标、复制、Markdown 导出与项目交接都会保留模式标记。
17. 续写检索若命中明确的“第 X 节 / 章 / 卷”，会额外带入相邻的下一篇章作为接续桥；问答检索不会带入相邻篇章，保持事实证据边界。
21. 依据面板相关查询由 `sourceQueryAllowsMessage()`、`composeSourceQuery()` 和 `sourceQueryForHistoryIndex()` 共同控制：问答只保留问答历史与章节定位，不拼接场景目标；续写才拼接当前场景信息。
22. `generateAssistantReply()` 重试旧回复时使用 `effectiveMode` 构造原作查询和降级回复；不要只让流式请求沿用旧模式，否则问答重试仍可能读到当前续写界面的场景信息。
18. `/api/source/outline` 只返回本机原作知识库的去重卷 / 节标题，过滤无章节标题文本生成的合成“作品开篇”标签；标题会写入当前知识空间的派生索引缓存，服务重启后也能复用；前端将其作为“当前章节 / 场景”的输入建议，不返回正文，不写入项目历史。
18a. `/api/source/chapter` 只接受当前空间的精确章节标题，返回最多 12,000 字的本机章节预览；它用于用户核对解析和事实边界，不写入项目 JSON、空间记忆或 GitHub。
19. 内容问答请求会过滤已保存且明确标记为非问答模式的历史用户提问和 assistant 回复；问答检索查询也会忽略非问答模式的旧用户提问，避免二创内容进入原作事实边界。
20. `source_search()` 会把“第19节”和“第十九节”归一化后再做卷 / 节定位，手动输入阿拉伯数字时也能命中原作标题。
   - 原作资料页提供章节导航：可以按卷名、章节号或标题筛选当前小说的章节标题，点击后直接同步到工作台；导航只返回标题，不会把原文全文写入前端。
   - 长篇或多卷小说的章节导航会额外提供“卷 / 分卷”筛选；先缩小卷范围再搜索章节号，可以降低重复章节编号造成的定位噪声。
23. 当查询同时包含章节标记和领域实体时，检索会优先保留章节标题 / 正文都共同命中的结果；没有联合命中时才回退到章节定位结果。
21. `source_citation_metadata()` 会识别“依据：/参考：”后的章节，也会扫描回答正文或括号中的明确卷 / 章 / 节标记；并用同一套数字归一化规则核对引用。
22. 前端对 `source_citation_status: "verified"` 显示“引用已核对”徽标，对 `unverified` 显示待核对警告，对问答中的 `none` 显示“引用未标注”；状态随重试版本和重新渲染同步。
24. `formatSourceAttribution()` 会把引用核验状态写入复制对话、Markdown 导出和项目交接摘要，verified / unverified 不只存在于当前页面。
25. 关系型检索使用独立的原作实体白名单，因此“方源”“方正”等两字人物名也会参与共同出现加权，但“重生”等概念词不会被误当人物；问答模式还会排除当前二创场景目标，只用问答历史与章节定位构造原作查询。
26. 多版本回复会按版本保存 `sourceRefsByVersion`、`sourceQueriesByVersion` 和 `sourceQualitiesByVersion`；切换重试版本时需同步当前来源元数据，依据面板和导出内容才能与当前文本一致。
9. 问答模式遇到模型服务失败时只生成明确的降级提示，并保留“查看依据”入口；前端可通过 `fetchSourceMetadataForFallback()` 回填本机章节标题和检索强度，但不能把本地检索片段伪装成模型总结或原作结论。
10. 降级回复仍会保留本次 `sourceQuery`，所以用户可以直接打开依据面板核对，但检索文本不会进入 `conversationHistory`。
11. 重试会过滤旧版本中 `source === "demo"` 的未生成提示；如果已有有效回复而本次重试失败，则恢复原回复，不把失败提示保存成新的正文版本。
11. `loadProjects()` 会识别旧版内置“红楼梦 / 林黛玉 / 贾宝玉”演示项目，在不删除旧项目的前提下新增并切换到《蛊真人》默认项目；不要扩大这个识别条件，以免误迁移用户自己的作品。
12. `renderModePrompts()` 会根据当前模式替换右侧三张内置灵感卡；问答模式提供人物、蛊虫和势力问题，项目自定义灵感卡不受影响。

## 服务端接口

- `GET /api/health`：读取当前服务配置、历史预算和请求超时状态，不发起模型请求
- `/api/health` 的 `provider_details` 会返回各服务缺失的中文配置项和环境变量键名，不包含密钥、端点或请求头值
- `GET /api/models`：读取模型列表；Azure 类型端点只返回配置中的部署名，并标记为非实际探测
- `POST /api/probe`：用最小请求验证当前服务的密钥、端点和模型部署；探测预算为 16 tokens，兼容需要少量隐藏推理预算的办公网模型
- 切换到已配置的 Ollama、OpenAI 或 OpenAI-compatible 时前端会自动读取模型列表；配置缺失时只显示修复指引，不发模型列表请求。办公网 Azure 不自动请求列表，继续使用本地配置的部署名
- `POST /api/summarize`：用当前模型把最近对话整理为可复用的剧情摘要；传入 `summary_target: "scene"` 时返回不超过 600 字的当前场景结果

模型请求和模型列表读取会先校验客户端选择的模型及本地环境变量；缺少配置返回 400 和可执行的中文提示。上游的认证、权限、模型不存在、限流、超时和连接失败会被服务端转换为安全的中文提示；响应状态码也会分别反映认证、权限、模型不存在（404）、限流或网关超时，便于定位配置问题。

模型服务区新增“连接诊断”面板，会读取 `/api/health` 展示各 provider 的配置完整度、缺少字段、缺少的环境变量键名、当前模型、请求超时和历史预算，并支持复制诊断文本；设置区主描述只显示可理解的缺少项和三步修复指引。所有输出明确排除 API key、端点和请求头值。

设置区的“复制缺少项”只复制当前 provider 缺少的环境变量名称，便于安全交接；没有缺失项时按钮隐藏。

项目菜单支持全量项目 JSON 备份，也支持从对话菜单导出当前单个项目 JSON；导入时两种格式都会被识别。导入前会确认数量并提示现有项目不会被覆盖。全量备份导入后会按备份里的 `activeProjectId` 恢复当时正在编辑的项目（若该项目不在导入范围，则回退到第一项）。

全量备份导入确认框会展开实际导入项目的简要预览，包括项目名、消息量、场景完成进度和草稿状态；最多显示前 4 个项目，并用星号标记备份时正在编辑的项目。

全量备份会附带 `novelSpaceBindings` 空间清单，单项目备份会附带 `novelSpaceBinding`；清单只包含空间 ID、名称、格式和可用状态，不包含小说原文、章节索引或空间记忆。导入时优先按空间 ID、再按名称匹配；缺少可用原文时项目先回退到当前空间，并在小说库展示持久化的空间恢复提示。

上传与备份清单同名的小说后，恢复提示会自动减少，并把此前因缺少原文而回退的项目及检查点重新关联到新空间；空间记忆和检索策略仍需在对应小说卡片单独导入空间配置。空间记忆读取失败时前端会阻止覆盖式保存，避免把暂时的读取错误当成空记忆；切换小说或刷新记忆时会显示“正在读取”，完成前也会阻止保存、删除和置顶，避免旧空间列表覆盖目标空间记忆。

小说异步解析任务的任务号和文件名会保存在浏览器本地；页面刷新后会先读取 `/api/novels/upload-status`，任务仍在运行就继续轮询，任务已完成则接收并建立知识空间，失败 / 取消 / 服务重启导致任务不存在则清理过期引用并提示重新选择原文件。文件本身不会写入浏览器存储。

空间记忆请求按小说空间分别维护读取计数和请求序号；连续刷新或快速 A → B → A 切换时，旧响应只会被丢弃，不会覆盖最后一次读取结果。记忆加载期间仍会阻止写操作。

如果上传文件名与一个“原文不可用”的已上传空间同名，前端会把该空间 ID 作为恢复目标传给服务端；服务端仅在名称匹配且旧文件确实缺失时复用原空间，并保留原空间记忆与项目绑定，同时在替换前使用临时文件和检索缓存清理，避免半成品文件或旧索引残留。

顶部新增命令面板入口，支持 `⌘ / Ctrl + K` 打开；可搜索并执行新建项目、快速保存检查点、打开场景计划、归档历史、上下文预览、连接诊断、状态处理、导出和专注模式等操作，支持 ↑ / ↓ 与 Enter 键盘导航。
- `POST /api/chat`：非流式回复
- `POST /api/chat/stream`：SSE 流式回复

聊天响应会额外返回服务端最终采用的 `source_query`、安全的 `source_references` 章节标题列表、`source_quality` 检索命中质量、`source_answer_coverage` 答案依据层级和 `truncated` 状态；流式 `start` 事件同样会返回两层依据状态，`done` 事件会标记是否因长度上限截断。前端分别显示“答案依据”和“检索相关性”，并按回复版本保存这些元数据；检索强度不再被当作答案事实置信度。`POST /api/source/search` 仅返回有限检索片段，不是原文下载接口，前端依据面板也不会把结果持久化。
原作依据弹窗在命中有限、单一命中或未命中时会提供其他检索策略按钮；点击后复用当前查询在弹窗内重试，避免用户关闭依据面板后再返回资料页修改策略。

支持 `custom_azure`、`ollama`、`openai`、`azure` 和 `compatible`。每个项目会按服务分别记住模型名，办公网端点与本地 Ollama 的配置示例都在 `.env.example`。OpenAI-compatible 只要求 Base URL，本地服务没有 API key 时会使用 `local`；远程兼容服务仍可填写真实 key。普通回复、流式回复和摘要都能处理字符串或结构化文本片段。服务端会过滤 `replace_with_your_key`、`your-deployment-name`、`your-office-endpoint.example` 等配置占位符，避免状态误报。
办公网 `X-TT-LOGID` 也会过滤 `${your_logid}` 形式的示例占位符；只有本机 `.env` 中填写真实值时才会发送该请求头。

## 当前功能

Markdown 导出会保留重试生成的备选回复，并在对话菜单中明确标注，便于交接和比较不同写法。

模型服务在请求阶段失败时，前端不会使用本地模板补写内容；失败消息明确显示“未生成”，操作只保留重试与模型设置，本机检索依据仍可核对。`source: "demo"` 继续作为旧数据兼容和失败状态标记，但这类消息会从后续实际模型上下文与上下文预览中排除，避免污染故事连续性。

模型设置区会根据当前服务显示不含敏感值的接入提示：Ollama 给出本地模型准备与默认地址，办公网 / Azure / compatible 给出所需环境变量方向；提示不会回显密钥或端点值。

模型设置还提供“复制配置模板”，按当前服务生成带安全占位符的 `.env` 片段；模板不包含真实密钥或内部端点。

模板弹窗支持“保存当前为模板”：会保存作品设定、角色卡、场景计划和灵感卡，最多 12 个自定义模板；自定义模板可删除，也会随全量项目 JSON 备份导入导出。

角色卡旁新增“角色库”：可保存当前选中角色、加入其他项目或删除库中条目，最多 24 个；支持按角色名、语气或人物设定搜索；加入同名但设定不同的角色时会确认是否覆盖当前项目卡片；全量备份格式已升级为 v4，旧版 v1 / v2 / v3 备份仍可导入。

新版默认隐藏右侧灵感栏；工作台会在当前问答 / 续写模式尚无用户任务且草稿为空时，通过 `renderTaskStarters()` 显示三条轻量快捷起点。点击调用 `fillPrompt()` 填入并触发 `input` 事件，因此草稿会立即持久化且快捷起点自动收起；不会自动提交模型请求。灵感库仍可通过对应管理入口使用；添加或编辑自定义灵感时勾选“同时保存到跨项目灵感库”，编辑已有项目灵感会同步更新对应库条目；其他项目可加入、删除；全量备份格式升级为 v4，旧版备份仍可导入。

导入全量备份时会在确认框显示备份版本，并说明会按当前版本可识别字段导入，便于判断旧版或未来版本文件的兼容范围。

对话菜单中的“查看归档历史”会打开只读搜索面板；归档消息可以按关键词筛选、复制或一键引用到当前输入框；当前对话搜索栏的“搜归档”会沿用同一关键词继续检索完整历史；归档中的角色回复还可以直接创建剧情支线，不会改变当前工作区的对话顺序。

归档面板的“清理归档”会在二次确认后删除当前项目的归档消息，保留工作区和摘要；操作前会提醒先导出 JSON 备份，以便释放本地存储空间。“重新开始”会同时清空工作区、归档、剧情摘要和草稿，保留作品设定、角色、场景计划、摘录和检查点，避免旧剧情继续进入新会话。任何从角色回复创建的支线都会清空旧摘要和草稿，并按消息节点裁剪带有来源标记的场景结果；历史项目中没有来源标记的场景结果会在消息分支中清除，优先避免把未来剧情带入新支线。

摘要和本幕结果提炼会按当前项目的工作区 + 归档总消息数判断是否有足够内容；工作区消息变短后仍可继续整理完整历史。

启用精简上下文前会检查 `summaryMessageCount` 与当前消息总数；摘要后有新增消息时会显示新增数量并二次确认，用户取消则继续保留完整上下文。

启用精简上下文时还会检查场景结果新鲜度；如果场景卡结果没有覆盖最新剧情，会在确认框中说明这些结果仍会随场景计划发送，用户可取消并先重新提炼。

检查点列表新增“支线”操作，可从任意检查点复制完整创作状态创建新项目；复制项目和从回复创建支线时会同步更新作品标题，避免后续保存时被原项目名称覆盖。

检查点卡片会显示保存时的场景、当前目标、角色和是否含草稿，帮助恢复或创建支线前快速确认快照内容。

项目切换区会显示当前项目谱系。复制项目、从检查点、工作区回复或归档回复创建支线时，会记录来源项目及具体派生节点；来源信息会随 JSON 备份和项目交接摘要迁移，旧项目和旧备份默认视为独立项目。

项目切换区还会显示当前项目状态：场景完成进度、摘要是否覆盖最新消息、待更新的本幕结果和未发送草稿；项目搜索会索引这些状态词，并提供“需要处理 / 摘要待更新 / 结果待更新 / 有草稿”一键筛选，下拉选项的悬停提示带上同一状态，方便在多个长篇项目间选择下一步。

如果搜索或状态筛选隐藏了正在编辑的项目，下拉框会保留一个禁用提示“当前项目未在筛选结果”，不会把第一个匹配项目伪装成当前项目。

项目状态旁的“去处理”会按优先级定位到待更新摘要、场景计划中的过时结果或未发送草稿；没有待处理项时按钮隐藏。

场景计划中的本幕结果会显示消息覆盖范围：如果结果之后又写了新剧情，会标记新增消息；如果结果来源已经不在当前历史或支线中，会提示重新提炼。没有来源标记的旧场景结果会显示为旧数据，不会被静默当作最新事实。

检查点卡片支持“改名”，只更新检查点标签，不会改变快照内容；适合在创作推进后补充分岔点、幕次或选择信息。

检查点弹窗顶部支持按名称、场景或角色搜索；搜索只影响列表显示，不会改变或删除任何快照。

检查点卡片的“对比”会打开当前进度与该快照的只读差异视图，展示消息数量、场景、角色、摘要覆盖范围、上下文模式、草稿、场景目标、场景结果及结果来源变化，并支持复制对比结果；不会修改任何快照或当前内容。

作品设定、当前章节 / 场景、可排序且会参与模型上下文的场景计划（目标、已发生结果 / 线索、进度摘要、本幕结果提炼；同一时间只保留一个“进行中”场景）、角色回复一键记为场景结果、场景完成并推进、专注模式下的当前场景提示、参考片段导入、剧情摘要新鲜度提示、可逆的精简上下文模式、上下文预览、角色卡（含人物设定、愿望、秘密和关系）、四套创作模板、续写/改写/独白、安全 Markdown 回复渲染、项目切换与分支、灵感摘录、命名检查点、Markdown 导出（包含重试生成的备选回复）、JSON 备份、模型流式生成、停止/重试、多回复版本、对话搜索、模型连接测试和上下文用量提示均已实现。每个项目工作区保留最近 120 条消息，较早内容会自动归档在项目数据中，最多保留 360 条；普通聊天仍遵循精简上下文开关并发送最近 20 条，剧情摘要和本幕结果提炼会读取工作区与归档历史，再按历史字数预算整理。作品设定输入采用短暂合并保存，切换项目、导出和离开页面时会立即落盘。前端发送前会与服务端对齐最近 20 条消息及历史字数预算，预览内容可作为排查上下文问题的依据。

项目切换区会显示本地项目数据用量；接近浏览器存储配额或保存失败时，会提示立即导出 JSON 备份。

推进下一幕前会检查当前场景结果是否存在且覆盖最新消息；缺失或过期时先给可取消提醒，用户仍可明确选择继续推进。

项目切换区的搜索支持作品名称、章节、当前场景卡、场景目标和角色名，并显示筛选数量以及当前项目的消息数、场景卡数。跨项目读取对话时会使用目标项目自己的工作区，不会误拼接当前项目的实时消息。

页面离开时会立即保存当前项目的所有表单字段，而不只是草稿；这覆盖了短暂合并保存窗口内的最后一次作品设定编辑。

Markdown 导出会先执行一次草稿同步，并在导出文件中附带当前未发送草稿；JSON 备份仍保留完整项目状态。

手动编辑剧情摘要不会在每次输入时刷新 `summaryMessageCount` / `summaryUpdatedAt`；编辑期间显示“摘要编辑中”，失焦或页面离开时才记录覆盖范围，避免未完成摘要看起来已经覆盖最新剧情。

提炼剧情摘要现在会打开只读的新旧摘要对比弹窗，显示字数和覆盖消息数；只有点击“替换摘要”才会更新当前项目，取消、点击遮罩或切换项目都会丢弃待确认结果。
摘要 / 本幕结果提炼期间，发送框会拒绝新消息；摘要预览应用前同时校验项目、消息总数和最新消息指纹，场景结果预览校验项目与最新消息指纹，生成预览后发生新剧情时必须重新提炼。
混合续写与问答时，`summary_target: "scene"` 会跳过带 `mode: "问答"` 的历史消息；`summary_target: "story"` 会给问答历史加“原作问答参考，不是剧情事件”标记，避免资料查询污染剧情沉淀。
切回创作模式时，`build_messages()` 会给带 `mode: "问答"` 的历史消息加“原作问答参考，不是剧情对话”标记，并在创作系统提示中明确禁止把问答提问 / 回答当作已发生剧情。

场景计划的当前节点与“当前章节 / 场景”字段保持一致：手动改写章节会解除旧节点的进行中状态，编辑当前场景为已完成或待写时会清除当前节点，避免模型收到与界面不一致的场景目标。

发送上下文中的场景计划超过 2000 字时，会优先保留当前场景、前后邻近卡片和计划首尾锚点，并注明中间省略数量；完整计划仍保存在本地，避免长篇大纲前半段挤掉正在写的场景目标。

场景计划弹窗支持按名称、目标或线索搜索，并按待写 / 进行中 / 已完成筛选；列表会显示筛选数量，但完成进度仍按全部场景卡计算。筛选只影响显示，编辑、排序、设为当前和删除仍使用原场景卡索引，不会误操作未显示的卡片。

“提炼本幕”生成的场景结果现在会先打开新旧记录对比弹窗，只有点击“替换结果”才会更新场景卡及结果来源标记；取消、点击遮罩或切换项目 / 场景都会保留原记录。

场景计划工具栏的“定位当前”会清除搜索和状态筛选，将列表滚动到当前场景卡并短暂高亮；没有当前场景时按钮会禁用。

侧栏场景信息旁的“复制计划”会将完整场景计划（作品、当前节点、各幕目标、状态和线索）复制到剪贴板，适合交给其他 agent 或粘贴进新的创作上下文。

上下文用量现在会分别统计作品设定、角色卡和实际发送的对话历史，并在上下文预览顶部展示分项与合计；精简上下文模式的标题提示也会带上同一组分项，便于定位预算消耗来源。

上下文预览顶部还会显示当前项目状态和新鲜度提醒，包含摘要后新增消息、待更新场景结果或“均已覆盖当前历史”，与发送前的精简上下文确认保持一致。问答模式的上下文预览和用量统计使用 `getModelPreviewContext()` / `getModelPreviewCharacter()`，只展示实际会发送的作品 / 章节定位和原作资料助手；创作笔记已被服务端隔离时，前端也不会继续把它们显示成发送上下文。`getPreviewModelMessages()` 还会同步服务端历史规则：问答模式过滤带创作模式标记的历史，创作模式给问答历史加“原作问答参考，不是剧情对话”标记，并据此计算预览字数。
上下文预览还会显示本次 `getSourceQuery()` 生成的原作检索查询，并明确说明服务端会动态加入有限证据片段；顶部消息计数必须使用 `getPreviewModelMessages()`，不能用未过滤的 `getModelMessages()`。
`getModelMessageSource()` 与 `getPreviewModelMessages()` 先复用相同的归档桥接 / 最近消息范围，再过滤模式并按 `INK_ECHO_HISTORY_BUDGET` 对齐预算；不要只修改其中一条路径。

对话菜单新增“复制项目交接摘要”和“下载项目交接摘要”，会将当前作品设定、参考片段、当前草稿、角色卡、场景计划、剧情摘要、最近 8 条对话、最近摘录和检查点整理成 Markdown；最近对话、参考片段和草稿分别最多截取 1200 / 2000 / 2000 字，摘要明确不包含 API key、端点或其他敏感配置，既可粘贴给下一位 agent，也可保存成文件交接。

项目数据标准化会区分“没有对话字段的旧数据”和“明确为空的对话数组”：旧数据会使用默认示例兼容迁移，导入或恢复的空项目则保持为空，不会被演示对话污染。

全量 JSON 导入会先过滤空值、数组和其他无效项目条目；确认框和完成提示会显示跳过数量，不会把损坏条目转换成新的空项目。

如果备份中的有效项目超过本地 50 个项目上限，确认框和完成提示会单独显示因容量未导入的数量，避免把容量截断误认为数据损坏。

检查点弹窗提供“快速保存”，会按当前场景和时间自动命名；全局快捷键 `⌘ / Ctrl + Shift + S` 也会触发同样的保存，不会覆盖已有检查点。

## 验证

```bash
node --check app.js
python3 -m unittest -q test_ecphory_memory.py test_memory_extraction_harness.py test_reviewed_memory_pipeline.py test_server.py test_frontend_contract.py
python3 -m py_compile ecphory_memory.py memory_extraction.py reviewed_memory_pipeline.py server.py scripts/memory_extraction_harness.py scripts/reviewed_memory_harness.py scripts/reviewed_memory_eval.py scripts/memory_revalidation_harness.py scripts/reviewed_memory_audit.py scripts/reviewed_memory_audit_repair.py scripts/reviewed_memory_qa_smoke.py
```

模型式原作记忆已完成仓库内生算法接入。`memory_extraction.py` 默认采用 `v9-strict-boundaries`，在逐字证据与独立模型审查之外增加动态覆盖提示、事实类别边界、关系方向、地点方向、低价值动作、模糊实体和不确定陈述过滤；`ecphory_memory.py` 将已审查事实构建为实体记忆痕迹、共现图和意图感知的多信号两跳检索，并保留章节位置与逐字依据。构建器会用当前规则重验历史报告，整库还会经过第二次独立审计和有证据约束的修复复核。当前《蛊真人》production 覆盖 20 章、97 条事实和 147 个实体痕迹，记忆版本 `d8d351d9443d45242e48cff8`；严格答案评测为 24/24，真实模型问答烟测为 3/3。产品只读取与当前原文 revision 完全一致的 production 索引，否则回退全文检索；未覆盖章节不应被描述为已经完成全书模型解析。详见 `MEMORY_EXTRACTION_EVAL.md` 与 `ECPHORY_MEMORY_ARCHITECTURE.md`。

任意上传小说也已接入同仓库后台构建路径。`reviewed_memory_pipeline.py` 负责均匀选择代表章节、执行 v9 抽取与独立审查、计算质量门禁和生成已核准 claims；`server.py` 负责持久化任务状态、逐章检查点、暂停 / 中断续建、质量失败重跑、原文 revision 锁和显式 pilot 推广。前端在“空间记忆 → 原作知识”提供单一状态卡，远端模型开始前明确说明最多 6 个代表章节会发送；原文问答无需等待，未通过或未启用结果不会参与问答。

当前服务端回归测试覆盖模型配置、请求路由、上下文预算、原作切分与检索、上游错误诊断和流式输出；另有前端契约检查覆盖问答模式、知识库状态和关键控件。GitHub Actions 还会启动本地服务进行首页、健康检查和静态资源白名单烟测。服务端普通聊天会在最近 20 条消息中执行历史预算，摘要任务会读取工作区与归档历史，默认预算 48000 字，可通过 `INK_ECHO_HISTORY_BUDGET` 调整并优先保留最新内容。发布前检查本地 `.env` 没有被加入版本控制，并确认 GitHub Actions CI 通过。
当前本地回归套件为 294 项测试；除原有模型、检索、上下文、前端契约和上传流程外，还覆盖实体关联两跳召回、组合关系路径、直接事实意图过滤、关系 / 地点 / 持有物边界、时间表达不冒充地点、答案正文与证据窗口分离、当前规则构建预检、整库二审、证据约束修复、章节截止过滤、持久化恢复、不可变记忆版本、通用后台采样与恢复、质量失败重跑、pilot 推广门禁以及正式问答优先读取已推广记忆。提交前至少运行上方验证命令，并确认 `/api/health` 返回的 `source.parse_diagnostics.structured_chunks` 与 `heading_coverage` 已随最新原文状态更新。

## 后续可选方向

- 为作品提供更多可复用的创作模板和示例
- 增加更细的上下文压缩与长篇项目管理
- 补充浏览器端自动化测试和视觉回归检查
- 评估是否需要账号同步或云端存储
